# Bridge Impact Fund Website

React and Vite site for the Bridge Impact Fund.

## Local Development

```bash
npm install
npm run dev
```

## Production Build

```bash
npm run build
```

## Vercel Deploy

Vercel will auto-detect the Vite project. The included `vercel.json` sets the build command and output directory explicitly.

```bash
npx vercel --prod
```
